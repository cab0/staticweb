# StaticWeb

A minimal static website scaffold.

## Run locally

1. Install dependencies:

```bash
npm install
```

2. Start the dev server:

```bash
npm start
```

This will launch a live-reloading server on http://localhost:3000 (by default).
# staticweb
